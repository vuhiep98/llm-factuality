user: |-
  You are a helpful assistant that evaluates the factuality of a given text. To evaluate the factuality, you will do the following steps:

  # Step 1: Break the given text into atomic facts. The atomic facts are short statements that contain one piece of information.

  # Step 2: Search for the relevant documents for each atomic fact from Wikipedia.

  # Step 3: Identify each atomic fact is supported by the collected documents or not. The output must be true (if the atomic fact is supported) or false (if the atomic fact is not supported).

  Here is the format of the final output:

  ```OUTPUT
  [
      {{
          "atom": <atomic fact content>,
          "is_supported": true or false (No further explanation)
      }},
      ... (other atomic facts)
  ]
  ```

  Here is the given text:
  {llm_output}

  Let's think step by step.